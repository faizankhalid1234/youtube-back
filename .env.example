# MongoDB connection string (required for Vercel - set in Project Settings → Environment Variables)
MONGODB_URI=mongodb://localhost:27017/youtube-clone

# For MongoDB Atlas (cloud): mongodb+srv://user:password@cluster.mongodb.net/youtube-clone

# Port for local dev (optional)
PORT=5000
